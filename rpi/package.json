{
  "name": "rpi",
  "version": "1.0.0",
  "description": "The argos module which runs on the in home device",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "argos",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.16.1",
    "express": "^4.14.1",
    "node-cron": "^1.1.3",
    "request": "^2.79.0"
  }
}
